import type { LngLat, LngLatBounds, PointGeometry, YMap } from '@yandex/ymaps3-types';
export declare function boundsToCornerPoints(bounds: LngLatBounds): {
    leftUpperCorner: LngLat;
    leftLowerCorner: LngLat;
    rightUpperCorner: LngLat;
};
export declare function leftUpperPointToPointGeometry(bounds: LngLatBounds): PointGeometry;
export declare function boundsToWidthHeight(root: YMap, bounds: LngLatBounds): {
    width: number;
    height: number;
};
